@page "/"

@using Microsoft.Extensions.Configuration
@inject IConfiguration config
@using Data;

<h1>Hello, world!</h1>

Welcome to your new app.

@code { protected override void OnInitialized()
    {
        base.OnInitialized();

        int val = config.GetValue<int>("WeatherForecast:Days");
        Console.WriteLine($"Got value from config-file (secrets.json override): {val}");

    }
}
